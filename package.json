{
  "name": "shopify-theme-store",
  "version": "1.0.0",
  "description": "Custom Shopify Theme",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "shopify theme dev",
    "deploy": "shopify theme push",
    "lint": "theme-check"
  },
  "keywords": [
    "shopify",
    "theme",
    "store"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@shopify/cli": "^3.0.0",
    "@shopify/theme-check": "^1.11.0"
  }
} 